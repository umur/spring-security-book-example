server:
  port: 8099

spring:
  application:
    name: security-headers-example
  datasource:
    url: jdbc:h2:mem:headersdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
