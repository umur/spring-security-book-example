server:
  port: 8091

spring:
  application:
    name: remember-me-example
  datasource:
    url: jdbc:h2:mem:rememberme
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
