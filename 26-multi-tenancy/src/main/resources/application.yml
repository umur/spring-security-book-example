server:
  port: 8106

spring:
  application:
    name: multi-tenancy-security-example
  datasource:
    url: jdbc:h2:mem:multitenancy
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
