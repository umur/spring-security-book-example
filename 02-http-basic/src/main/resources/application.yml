server:
  port: 8082

spring:
  application:
    name: http-basic-example
  datasource:
    url: jdbc:h2:mem:httpbasic
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
