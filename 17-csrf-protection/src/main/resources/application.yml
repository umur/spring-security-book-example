server:
  port: 8097

spring:
  application:
    name: csrf-protection-example
  datasource:
    url: jdbc:h2:mem:csrfdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
