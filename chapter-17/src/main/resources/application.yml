spring:
  application:
    name: cinetrack-ch17
  datasource:
    url: jdbc:h2:mem:cinetrack;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

server:
  port: 8080
