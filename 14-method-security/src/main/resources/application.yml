server:
  port: 8094

spring:
  application:
    name: method-security-example
  datasource:
    url: jdbc:h2:mem:methodsecuritydb
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
