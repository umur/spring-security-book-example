server:
  port: 8081

spring:
  application:
    name: form-login-example
  datasource:
    url: jdbc:h2:mem:formlogin
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
