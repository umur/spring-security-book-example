server:
  port: 8098

spring:
  application:
    name: cors-configuration-example
  datasource:
    url: jdbc:h2:mem:corsdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
