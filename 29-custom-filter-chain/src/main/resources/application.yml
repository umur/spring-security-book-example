server:
  port: 8109

spring:
  application:
    name: custom-filter-chain-example
  datasource:
    url: jdbc:h2:mem:filterchain
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
