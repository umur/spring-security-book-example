server:
  port: 8108

spring:
  application:
    name: password-encoding-example
  datasource:
    url: jdbc:h2:mem:passwordencoding
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
