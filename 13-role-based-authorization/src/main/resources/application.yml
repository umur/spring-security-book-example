server:
  port: 8093

spring:
  application:
    name: rbac-example
  datasource:
    url: jdbc:h2:mem:rbacdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  h2:
    console:
      enabled: true
